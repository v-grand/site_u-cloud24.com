# Cloudflare Pages Redirects Configuration
# For Single Page Application (SPA) routing

# All requests should be served by index.html (SPA routing)
# Except for static files and API routes
/* /index.html 200

# Specific static files and assets
/robots.txt /robots.txt 200
/sitemap.xml /sitemap.xml 200
/favicon.ico /favicon.ico 200

# Cache control headers can be added with _headers file
